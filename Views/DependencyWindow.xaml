<Window x:Class="DupFree.Views.DependencyWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dependency Status" Height="400" Width="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Text="Outdated packages (run using `dotnet list package --outdated`):" FontWeight="Bold" />
        <TextBox Grid.Row="1" x:Name="OutputText" IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" />
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,8,0,0">
            <Button x:Name="AutoUpdateButton" Content="Auto-update" Width="100" Margin="0,0,8,0" Click="AutoUpdateButton_Click" />
            <Button Content="Copy" Width="80" Margin="0,0,8,0" Click="CopyButton_Click" />
            <Button Content="Close" Width="80" Click="CloseButton_Click" />
        </StackPanel>
    </Grid>
</Window>